/**
 * Contact / CTA Section — PLACEHOLDER
 * ─────────────────────────────────────
 * Full implementation in next sprint.
 * Layout: 100vh, centred CTA with animated lift,
 *         optional contact form or enquiry link.
 * Animations: Scroll-velocity-aware background overlay,
 *             Framer Motion CTA lift on scroll entrance.
 */
export function Contact() {
  return (
    <section
      id="contact"
      className="relative w-full flex items-center justify-center"
      style={{
        minHeight: '100svh',
        background: 'var(--color-bg)',
        borderTop: '1px solid rgba(197,160,89,0.08)',
      }}
    >
      {/* Content will be built in the Contact sprint */}
    </section>
  );
}

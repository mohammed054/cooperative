/**
 * About / Brand Story Section — PLACEHOLDER
 * ─────────────────────────────────────────
 * Full implementation in next sprint.
 * Layout: 100vh, left text column + right visual.
 * Animations: GSAP stagger reveal, subtle parallax background.
 */
export function About() {
  return (
    <section
      id="about"
      className="relative w-full flex items-center justify-center"
      style={{
        minHeight: '100svh',
        background: 'var(--color-bg)',
        borderTop: '1px solid rgba(197,160,89,0.08)',
      }}
    >
      {/* Content will be built in the About sprint */}
    </section>
  );
}

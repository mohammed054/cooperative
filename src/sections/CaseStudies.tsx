/**
 * Case Studies / Portfolio Section — PLACEHOLDER
 * ───────────────────────────────────────────────
 * Full implementation in next sprint.
 * Layout: 100vh, horizontal card scroll or grid reveal.
 * Animations: GSAP stagger cards, depth shadow on entrance.
 */
export function CaseStudies() {
  return (
    <section
      id="work"
      className="relative w-full flex items-center justify-center"
      style={{
        minHeight: '100svh',
        background: 'var(--color-bg)',
        borderTop: '1px solid rgba(197,160,89,0.08)',
      }}
    >
      {/* Content will be built in the Case Studies sprint */}
    </section>
  );
}

/**
 * Testimonials / Trust Section — PLACEHOLDER
 * ─────────────────────────────────────────
 * Full implementation in next sprint.
 * Layout: 100vh, staggered quote reveal, parallax background.
 * Animations: Framer Motion per-testimonial entrance.
 */
export function Testimonials() {
  return (
    <section
      id="testimonials"
      className="relative w-full flex items-center justify-center"
      style={{
        minHeight: '100svh',
        background: 'var(--color-bg)',
        borderTop: '1px solid rgba(197,160,89,0.08)',
      }}
    >
      {/* Content will be built in the Testimonials sprint */}
    </section>
  );
}
